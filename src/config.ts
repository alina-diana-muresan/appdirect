'use strict';

/**
 * module holding the default application configuration
 */
module Config {
    export const USER_LIST: string[] = ['AppDirect', 'LaughingSquid', 'TechCrunch'];
    export const TWEET_COUNT: string = '30';
}

export = Config;
